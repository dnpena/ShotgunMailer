%h1 Listing users

%table.table
  %tr
    %th First name
    %th Last name
    %th Email
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.first_name
      %td= user.last_name
      %td= user.email
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to new_user_path, :class => 'btn btn-success btn-lg' do
  %i.fa.fa-plus
  =t('modules.register.title')